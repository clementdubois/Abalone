/*
git pull <- récuperer last version

git add .

git status <- voir si y'a eu des modifs

git commit -v -m "sauvegarde" <- nom donné à la sauvegarde.

git push <= pour le mettre sur le serveur

commit plusieurs en local sans le mettre sur le serveur tout de suite => mettre sur le serveur que à la fin de la journée
*/